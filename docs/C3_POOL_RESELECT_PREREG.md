# C3 池重选实验预登记（草案 v1，待补采完成后评审冻结）

> 创建：2026-09-19 ｜ 状态：**草案——数据补采在途，阈值待本仓数据复核后冻结**
> 依据：Hermes R9/R10 §C-C3 + docs/EXPANSION_MEMO.md（CAGR 判据已命中，
> 扩池评估触发成立）+ docs/audit/c3_data_requirements_20260919.md（数据盘点）
> + 三臂影子检验（幸存者偏差 2.45pp/年、现池构造成本 4.27pp/年）。
> ⛔ 判据预先固定、不许事后放宽；本实验不承诺晋级，只为测量「换池」的边际贡献。

## 一、研究问题

现池 487 只并非高股息逻辑构建（与 ≥3% 全 A 高股息池重叠仅 9.7%，R7 实证），
且静态池含幸存者偏差（alive_universe ∩ 静态目录 ⇒ 2.45pp/年虚高）。
**C3 = 按 P1 规格在 PIT 全 A 宇宙上重选年度池**，测冠军构型（e8b 同参）
在重选池上的边际收益，对照锚 = isst-e8b（同 SHA 同数据、旧池）。

## 二、宇宙口径（预先固定）

- 基础宇宙：bar 宇宙 5473 = market-breadth-a/daily_bars（5220 在市）
  ∪ delisted_bars（253 退市）——**含退市票，去幸存者偏差**；
- PIT 成员资格：票于日 t 属宇宙 ⟺ 当日在市（alive_universe ∩ bar 存在），
  退市票在退市前正常参与（历史年份照常在册）；
- 重选节奏：**年度重选**——每自然年**首个交易日**快照定当年池
  （与 P1「每年末快照定次年池」PIT 等价：首日的可得信息即上年末信息，
  且自然覆盖 2015 年——2014 末无 daily_basic 数据，用 2015-01-05 首日
  快照；⛔ 不许用年末数据回选当年，那是前视）。年内不变；
- 行业标签：用 `data/pool_meta/stock_industry.parquet` **当前快照**（无 PIT
  数据源——tushare 不提供行业变迁史）。**登记简化**：历史年份用当前
  行业标签，偏差量级小但存在；缺失行业记录按「未知行业」单列计数
  （在市 0 缺失、退市侧 3 只无记录 + 333 空值行全 D 状态）。

## 三、P1 七规则 → 本仓口径映射（阈值待复核冻结）

| # | 规则 | 本仓字段 | 口径 |
|---|---|---|---|
| 1 | dv ≥ 3%（年末快照） | daily_basic dv_ratio（R7 分片，全 A 2431 日） | 年末最后交易日 dv |
| 2 | ROE > 0 | financial_pit_alla.roe | 最近已公告年报（pub_date ≤ 快照日） |
| 3 | 净利润同比 > 0 | financial_pit_alla.net_profit_yoy | 同上 |
| 4 | 资产负债率 < 80% | financial_pit_alla.debt_to_assets | 同上 |
| 5 | 支付率 ∈ (0,1) | dividend_events_alla.Σcash_div / financial_pit_alla.eps | 最近完整分红年度口径（冻结时定税前/税后） |
| 6 | 近一年跌幅 >50% 剔除 | 日线 close | 快照日对一年前收盘收益 < −50% 剔除 |
| 7 | 行业权重 ≤ 20% | stock_industry（快照标签） | 池内行业占比超限 → 按 dv 降序截断 |

⛔ 阈值数字（3%/80%/(0,1)/50%/20%）源自 Hermes P1 规格——**冻结前须在
本仓数据上复核合理性**（例：支付率 0/1 边界、行业 20% 在 110 行业下的
实际约束力），不许照搬即跑；复核结论作为本文件附录登记，判据不变。

## 四、实验设计（预先固定）

- 池构建：`scripts/lab/c3_pool_rebuild.py`（待写）→ `data/c3_pool/
  pool_yearly.parquet`（{year, symbols[]}，确定性、幂等、manifest 哈希）；
- 回测构型：**e8b 冠军同参**（d=0.25/a=0.35/m=0/i=1 + demote + GC001），
  ⛔ 不动策略参数——变量只有「池」；
- 数据面：全 A bar（market-breadth-a 已有）+ dv_ratio 分片 + 新 exdiv/财务；
  universe_provider 改为按年查 pool_yearly（实现细节随池构建脚本评审）；
- 对照锚：`isst-e8b`（同 SHA/同 isST 数据/旧池）——差值 = 纯池效应；
- 辅助观测：年度池规模序列、池-旧池重叠率、行业分布、池内持仓数
  （容量体检：若 P1 池 <~50 只，持仓集中度风险须登记）。

## 五、判据（预先固定，收单时按此裁决）

- 主判据：CAGR 差 vs isst-e8b——
  - Δ ≥ +1.5pp 且 MDD 不劣化 >3pp ⇒ **池重选有效**，进 G-2/门禁流程；
  - Δ ∈ (−1.5pp, +1.5pp) ⇒ 边际不显著，登记归因后**弃**（不追加调参）；
  - Δ ≤ −1.5pp ⇒ 判负，P1 规格降级为研究记录；
- 守门判据：年化换手 ≤ 8（同 e11 口径）；持仓天数占比非病态
  （择时层行为不解释池效应）；池规模年均 ≥30 只（否则登记容量告警）；
- 交叉验证（只做一次、不迭代）：若主判据落在 ±0.5pp 模糊带，
  允许**唯一一次**分年一致性核查（10 年中 ≥6 年同向 ⇒ 判有效）。

## 六、基线变更登记模板（收单时填）

| 项 | 旧值 | 新值 | 变更原因 |
|---|---|---|---|
| universe_hash | （旧池） | （新池） | C3 池重选 |
| 池规模/构成 | 487 静态 | 年度动态 N | P1 七规则 |
| 对照锚 CAGR/MDD | isst-e8b | C3 | 池效应测量 |

## 七、诚实边界

- 行业标签为当前快照（无 PIT 源），历史行业映射有偏差；
- 支付率口径（税前/税后、分红年度归属）冻结时定，实现后不回改；
- P1 阈值来自外部报告，本仓复核只验「可执行性/量级」不验「最优性」；
- 池重选不解决阈值稳健性（G-2）问题——两线独立验收（C3 修池、
  G-2 修参数）；
- 年度重选+年内不变是 P1 规格；滚动/季频重选是另一实验，不在本登记。
