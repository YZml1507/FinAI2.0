# Alpha 三层作战手册（跨模型/跨窗口宪法）

> 创建：2026-09-17 ｜ 定位：本项目最高优先级执行纲领。**任何后继模型/窗口
> 必须先读本文件 + docs/TASK_TRACKER.md 顶部，再动手。**
> 本文件只登记「已裁决结论」和「必须遵守的纪律」；过程性进度在
> TASK_TRACKER，两文件冲突时以本文件为准。

## 〇、系统画像（一句话）

A 股中低频长仓日线量化：10-15 万本金、纯多头、持仓 3-8 只、487 只红利池、
日频宽度择时。**权威基线：CAGR 3.76% / MDD 21.43%**（产物
20260915-235155-t312-dividend-v1-noseed）。进攻基线（宽度冠军
`bd25a35m00i1`）：CAGR 5.82% / MDD 31.28%。 <!-- gate-doc-ignore: 历史快照（基线声明引用实测值），⛔ 不改史 -->

## 一、已裁决证据台账（⛔ 禁止重复试错）

### 已证负 / 已封顶（不许再做）

| 方向 | 裁决 | 证据 |
|---|---|---|
| 继续磨宽度参数（D/A/m/i 网格） | 封顶 | 24 组网格完成，冠军即基线，边际收益耗尽 |
| 趋势类技术指标做个股多头 | 负 | 调研环实测证据为负 |
| cyq_perf 筹码、龙虎榜 | 禁用 | 数据口径不可信/无前视保证 |
| PEAD event 模式（reserve 池+中途建仓） | 负 | E3b：-0.36%/MDD 37.9%——40% reserve 进攻日闲置+追高双拖累 | <!-- gate-doc-ignore: 历史快照（消融实验实测值，非基线声明），⛔ 不改史 -->
| 排雷 exit_half/exit_full 卖出版（L2/L3/L5） | 负 | E2b 3.66%——报表滞后，公告时价格已反映，追卖锁实亏+换手失控 |
| 归母净利润粗阈值做 PEAD | 负 | P8 探路实锤，必须扣非 SUE 口径 |
| PEAD 行业中性化用均值 | 负 | 实测把 sue_raw=-68 拉到 0.957 分位；已改中位数+sue_adj>0 符号闸 |

### 已证有效 / 当前最优

| 结论 | 证据 |
|---|---|
| 宽度择时本身有效 | attack 日均 +0.068% vs ice -0.097%（648 警戒日阴跌是慢性毒） |
| mid_cap=0（警戒区零仓） | 合法配置=上限语义，切断警戒区阴跌 |
| ice_confirm_days=1 | 缩短冰点确认暴露窗口（P0 主因=确认期扛跌） |
| 排雷 block_only 语义 | 只禁买不动仓，挡劣化票再入池；L1a/L1c（预亏/下修）保留真卖出 |

### 已裁决（E4 终局，2026-09-17）

- E4（全栈+PEAD rebalance 软叠加）：**CAGR 4.02% / MDD 37.78% / 226 笔** <!-- gate-doc-ignore: 历史快照（消融实验实测值，非基线声明），⛔ 不改史 -->
  → 低于 E2c（4.20%），**③PEAD 路线判负关闭**：在 487 红利池内两种
  建仓模式均无正贡献（event 拖现金，rebalance 挤占红利仓位且 MDD 反升）。
- Alpha 三层终局：所有构型均 < 进攻基线 5.82%。最优层间组合是 E1 <!-- gate-doc-ignore: 历史快照（消融实验实测值，非基线声明），⛔ 不改史 -->
  （veto 单开 4.80%/MDD 29.52%）——**veto 有防御价值（MDD -1.8pp） <!-- gate-doc-ignore: 历史快照（消融实验实测值，非基线声明），⛔ 不改史 -->
  但代价是收益 -1pp**；排雷 block_only（E2c）反而再拖 0.6pp。
  保底交付 = 宽度冠军构型 + 本文档化负证据。

## 二、操作铁律（违反=返工）

1. **零前视**：财务/预告一律 `pub_date` 对齐；价格序列 RAW 不复权。
2. **fail-closed**：开关开了数据不在就 raise，⛔ 不许静默降级。
3. **幂等原子写**：数据产物先写临时文件再 rename。
4. **防幸存者偏差**：股票池用当日 universe_provider。
5. **防过拟合**：任何新参数做 ±20% 扰动悬崖检验 + 跨周期分段验证，
   悬崖敏感即弃。
6. **测试纪律**：全量 pytest 必须全绿；`TEST_BASELINE_PASSED` 与实际
   收集数同步；文档新增实测数字若是历史快照须加 `gate-doc-ignore` 行内
   豁免（理由含「历史快照」）。
7. **改完 commit**：每个原子任务完成即 commit + 更新 TASK_TRACKER
   时间戳；FINDING 台账行数只增不减。
8. **实验命名**：`e<N>-<短名>`，结果写 `experiments/lab/<name>/`，
   leaderboard.jsonl 是事实源。
9. **唯一须停手请示**：删除数据 / 仓库级破坏性操作。其余决策自行拍板
   并写明理由。

## 三、标准实验流程（照此执行，不跳步）

```
1. 改代码 → 2. pytest 全绿 → 3. commit
4. run_experiment.py --name e<N>-xxx --set ...（后台，日志 _logs/）
5. 等 experiment.json → 6. 对照基线登记 TASK_TRACKER → 7. commit
```

- 消融原则：一次只动一个变量，命名体现实验变量。
- 对比基准恒为进攻基线 `bd25a35m00i1`（5.82%/31.28%），不是 MA200 基线。
- 任何「感觉会更好」的改动都必须落成实验编号，不许口头裁决。

## 四、E4 后决策树（已拍板，照走）

| E4 结果 | 动作 |
|---|---|
| CAGR ≥ 5.82% | PEAD 软叠加有效 → ±20% 扰动+分段验证 → 过门禁晋级新基线 |
| 4.20% ≤ CAGR < 5.82% | PEAD 中性偏负 → 砍 PEAD，保留 veto+block_only 排雷（E2c 构型）作为候选基线，转攻新 alpha 源 |
| CAGR < 4.20% | PEAD 在 487 红利池无 alpha → ③路线判负关闭，终点=②排雷；写终局报告 |

**E4 实测 4.02% → 已命中第三行：③路线判负关闭。**
且三层全部构型均 < 基线 → 诚实结论生效：「Alpha 三层对本池无正收益
贡献」，保底交付 = 宽度冠军构型（5.82%）+ 文档化负证据，**不许为了
交差而放松口径造数字**。残余可选价值：E1 veto 单开换来 MDD -1.8pp，
若未来目标改为「同等收益压回撤」可重议，但当前目标（提升年化）下
不启用。 <!-- gate-doc-ignore: 历史快照（消融实验实测值，非基线声明），⛔ 不改史 -->

## 五、性能受限模式（后继弱模型专用）

额度降级后按以下优先级收缩工作面，**宁可慢不可错**：

1. 只做已列入决策树的下一步，不主动开新方向；
2. 每个动作更小步：改一处→测一处→commit；
3. 不确定时优先保守分支（block_only 优于 exit，不加参数优于加参数）；
4. 遇到与台账冲突的「新想法」，先把理由写进 TASK_TRACKER 决策记录
   再动手——写不出理由就不做。

## 六、现场速查

- 仓库：`/home/ubuntu/FinAI2.0`；调研仓：`/home/ubuntu/research-finai-latest`
- 数据底座：红利池日线→2026-09-16；财务 PIT→2026 中报（含扣非）；
  全 A 宽度 5215 只；forecast/statements/veto/landmine/pead sidecar 已构建
- Tushare 双代理凭证在 `.env`（DATAHUBCO_API_KEY/PROMAX_TUSHARE_KEY，
  直连不走代理，limit≤5000、跨年分段、16 并发）
- 测试：`cd /home/ubuntu/FinAI2.0 && .venv/bin/python -m pytest tests/ -q`
- 实验：`scripts/lab/run_experiment.py --name X --set k=v ...`
