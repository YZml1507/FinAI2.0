# R8 数据口径核验报告（TASK_TRACKER 接续队列 ②）

> 日期：2026-09-19 ｜ 执行：本仓接续窗口
> 范围：接续队列第 ② 项——「校验指数日期顺序、代码映射及 ETF NAV/分拆口径」
> 审计脚本：`scripts/lab/audit_data_caliber_20260919.py`（可复跑，只读数据层）
> 结论一句话：**本仓落盘数据无 R8 告警的降序/反序收益问题；唯一确认的坏值
> 是 `sh.513100` 的 factor（2022-01-14，=5.0019 伪分拆），已登记隔离、
> 不改原始数据；R8 报告中「515100」系代码笔误。**

## 一、全收益指数 parquet（e9 曾拟用，现作基准参照）

| 文件 | 行数 | 区间 | 行序 | pct_chg vs close 升序重算 | 重复日 | 空值 |
|---|---|---|---|---|---|---|
| `data/macro/index_H20955_total_return.parquet` | 2676 | 2014-01-02 ~ 2024-12-31 | ✅ 升序 | 相关系数 = 1.000000 | 0 | 0 |
| `data/macro/index_H00922_total_return.parquet` | 2676 | 2014-01-02 ~ 2024-12-31 | ✅ 升序 | 相关系数 = 1.000000 | 0 | 0 |

- R8 告警的「R7 CSV 行序降序、pct_chg 反序」问题**仅存在于 R7 调研目录的
  CSV 中间产物**；本仓 2026-09-17 自采落盘的 parquet 为升序且 pct_chg 与
  close 逐日一致，未受污染。
- 十年口径复核（2015-01-05 ~ 2024-12-31，由 close 升序重算）：
  H20955 CAGR 9.59%、H00922 CAGR 9.35%——与 R7 登记的 9.7%/9.4% 口径一致
  （差异来自起止日舍入）。分年收益（H20955 / H00922，%）：
  2015 +28.78/+29.89；2016 +2.11/−4.30；2017 +15.50/+21.34；
  2018 −14.12/−16.15；2019 +16.88/+20.88；2020 +2.34/+8.18；
  2021 +19.27/+18.19；2022 +0.77/−0.37；2023 +13.26/+6.34；2024 +18.64/+18.76。

## 二、行情基准 parquet

| 文件 | 行数 | 行序 | pct_chg 口径核验 |
|---|---|---|---|
| `data/macro/index_000300.parquet` | 2431 | ✅ 按日期升序 | 与 close/pre_close 一致 |
| `data/macro/benchmark_510880.parquet` | 2431 | ✅ 按日期升序 | 与 pre_close 口径完全一致（见下） |

- 510880 的 pct_chg 与「close 简单环比」有 10 处偏差（>0.05pp），全部落在
  **每年 1 月除息日**；与 pre_close 口径（除息日 pre_close 已含除息调整）
  相关系数 0.9999999998、最大偏差 5e-05。**属正确行为，非坏值**。
- 两文件的 DataFrame 标签呈倒序（243→0）是写出时的索引残留，**行物理顺序
  按日期升序**，`diff()` 全正已验证。

## 三、ETF factor 口径（etf_bars，RAW + factor 列）

| 标的 | 核验结论 |
|---|---|
| `sh.512890` | 2021-10-25 factor 1.0→2.0462 = **真实 1:2 份额分拆**（close 1.639→0.801 同步减半）✅ |
| `sh.510500` | 2015-04-15 factor→0.2803 = **真实份额拆分**（close 2.243→7.818 同步）✅ |
| `sh.511990` | factor 全列 NaN——货基设计内行为（`collect_defensive_etfs.py` 明示留 NaN）✅ |
| **`sh.513100`** | **2022-01-14 factor 1.0→5.0019 伪跳变**（close 5.192→1.015 无真实分拆对应，NAV 连续）⛔ 坏值 |
| `sz.159915` / `sh.510300` | factor 无异常跳变 ✅ |

**坏值处置（隔离不覆盖，遵循 R8 转述的红线）**：
1. `data/etf_bars/sh.513100/2022.parquet` 的 factor 坏值**不改写原始数据**，
   在本报告登记即完成隔离标记；
2. 影响面核查：全仓代码无任何路径消费 513100 的后复权链——唯一消费
   `close×factor` 的是 `scripts/diagnose_t312_full_period.py`（诊断脚本，
   其报告已注明「非全收益」口径且未用于任何晋级/基线断言）；
3. R8 报告与本仓 TASK_TRACKER 中「515100 的 etf_bars factor 坏值」的表述
   系**代码笔误**：本仓 etf_bars 未采集 515100（R8 的 515100 数据在调研目录
   `subB/nav_515100.csv`，与本仓无关）；真实坏值在 **513100**。
4. 若未来启用 513100 做回测（如纳指候选另行立项时），须先修复 factor 链
   或改用 NAV chain，并在预登记中声明口径。

## 四、GC001 利率序列

`data/rates/gc001_daily.parquet`：2735 行，2013-08-27 ~ 2024-12-31，升序、
无重复日、无空值、无负值；年化利率区间 0.02% ~ 53.44%（月末/季末脉冲符合
GC001 历史特征）。×0.80/×0.90/×1.10/×1.20 扰动序列同目录在册。

## 五、结论与后续

- 接续队列 ② **完成**：指数日期顺序✅、代码映射✅（含 R8 笔误更正）、
  ETF 分拆口径✅（512890/510500 真实分拆正确处理，513100 坏值登记隔离）。
- H20955/H00922 序列质量合格，可作为**基准参照**继续使用；e9 方向本身
  维持 R8 否决状态不变。
- 本报告全部数值为 2026-09-19 对当前数据层的实测快照，数据文件后续若
  增量更新，须重跑 `scripts/lab/audit_data_caliber_20260919.py` 复核。
