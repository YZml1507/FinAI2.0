# 程序化交易系统架构说明书（系统说明书）

> **文档版本**: v1.0.0  
> **更新日期**: 2026-09-07（原文）｜**更正与复校日期**: 2026-09-10  
> **依据文件**: 《A股程序化交易监管细则与个人报备实操指南》（11 号调研报告）、证监会令第 220 号、上证发〔2023〕144 号  
> **适用范围**: 个人程序化交易报备材料必须项（清单必须项 #3）  

> ### ✅ 本文状态：**已按实现与权威产物更正（2026-09-10）**
> **本次更正项（文档 ↔ 代码/产物一致性，逐条可核）**：
> 1. **单测基线**：原文所载 `717 passed` 已过期 → **基线真值以 `scripts/gates/constants.py` 的 `TEST_BASELINE_PASSED` 为单一事实源（⛔ 文档不硬编码具体数值）**；
> 2. **ST 口径**：原文称「排除 ST」——⛔ 代码**无 ST 剔除逻辑**（`strategy/candidates.py::_select_stocks` 仅按股息率与市值筛选；`data/cleaner.py:317` 只用 `isST` 推导 ±5% 涨跌停档），已如实更正；
> 3. **参与率**：原文称「单笔委托 <= ADV 2%」——代码实为「≤ **当日成交额 5%**」（`strategy/portfolio.py:63` 的 `max_participation_rate`），已按代码更正；
> 4. **告警通道**：原文称「飞书 Webhook 实时告警」——⛔ 尚未实现（仅 `data/collector.py` 的 stub，只记日志不真发；`ops/` 下无告警模块），已如实标注；
> 5. **门禁数量与状态**：原文 `24 道全绿` —— 数量**真值以 `scripts/gates/gate_master_audit.py` 的门禁注册表为单一事实源**（⛔ 不硬编码具体数值；2026-09-10 当日实测快照 29——工程侧新增 `G-REPRO-1`，故不写死）；实测状态 12 PASS / 13 INCONCLUSIVE / 3 FAIL，⛔ 原「24 道全绿」不成立；
> 6. **单日下单笔数硬顶**：原文称「<=100 笔硬顶」——⛔ 代码中**未找到**该硬顶参数，已如实标注为未实现（实证笔数由持仓 3~8 只自然约束）。
> **仍不可提交的部分**：本系统策略的权威回测产物**未通过**回撤上限门禁 `G-MDD-1`（实测 43.08% 高于 35% 上限），且 Phase 4 为「⏸ 暂停，待策略 v2 落地后重审」，故本说明书**不得作为任何准入依据**。

---

## 1. 系统基本信息

- **系统名称**: FinAI2.0 A 股中低频量化交易系统（日线纯多头）
- **技术语言与版本**: Python 3.11.5 (64-bit)
- **部署环境**: 单机离线工作站（本地物理部署，无外部云主机中继，直连券商 QMT/PTrade 终端）
- **开发与版本管理**: Git 分布式版本控制（代码仓 `FinAI2.0` 与 计划仓 `research-finai`）
- **当前稳定代码版本**: Commit `4878ffe`（自动化测试基线**真值以 `scripts/gates/constants.py` 的 `TEST_BASELINE_PASSED` 为单一事实源**；⛔ 文档不硬编码具体数值——原文所载 717 为过期值）
- **核心定位**: 散户 10~15 万初始资金、纯多头、低换手（年化 <400%）、零做空、无高频交易特征（报单速率 <1 笔/分钟）

---

## 2. 系统六层总体架构

```mermaid
flowchart TD
  subgraph DATA_LAYER["1. 数据层 (Data Layer)"]
    D1["BaoStock 主数据源"] --> D3["PIT 财务与分红对齐"]
    D2["腾讯/新浪 校验源"] --> D4["除权与停牌清洗"]
    D3 & D4 --> D5["本地 Parquet 分区存储"]
  end

  subgraph ENGINE_LAYER["2. 回测与撮合引擎 (Engine Layer)"]
    E1["事件驱动时钟 (SDD-1)"] --> E2["七态订单状态机 (Order FSM)"]
    E2 --> E3["8 步撮合规则 (涨跌停/停牌/整手/T+1)"]
    E3 --> E4["七科目费用模型 (5元佣金地板/印花税/过户费)"]
    E4 --> E5["分厘级双账本 (Ledger and BookView)"]
  end

  subgraph STRATEGY_LAYER["3. 策略与组合层 (Strategy Layer)"]
    S1["全市场股票池初筛 (股息率>=3%, 停牌剔除; ST 不剔除)"] --> S2["高价股过滤 (close <= 300元)"]
    S2 --> S3["自由流通市值加权分配"]
    S3 --> S4["沪深300 MA200 择时空仓避险"]
    S4 --> S5["持仓锁定 3~8 只 / 单票>=2万元"]
  end

  subgraph RISK_LAYER["4. 事前风控层 (Risk Management Layer)"]
    R1["下单笔数由持仓 3~8 只自然约束 (⛔ 无 100 笔硬顶实现)"]
    R2["单笔委托 <= 当日成交额 5%"]
    R3["跳空缺口滑点防御 (Gap Slippage)"]
    R4["5% 现金交收缓冲"]
  end

  subgraph OPS_LAYER["5. 运维与台账层 (Ops Layer)"]
    O1["告警通道 (飞书: ⛔ 尚未实现, 仅 stub)"]
    O2["日终对账与净值记录 (Daily Tasks)"]
    O3["14号台账保鲜与到期提醒"]
  end

  subgraph GATE_LAYER["6. 六维防伪门禁层 (Quality Gates Layer)"]
    G1["D-Gate: 数据真值与反未来"]
    G2["L-Gate: 存活与参数落地"]
    G3["E-Gate: 引擎撮合与不可逆"]
    G4["A-Gate: 会计分厘与红利税"]
    G5["S-Gate: 科学统计与多重检验"]
    G6["G-Gate: 治理签名与防篡改"]
  end

  DATA_LAYER --> ENGINE_LAYER
  ENGINE_LAYER --> STRATEGY_LAYER
  STRATEGY_LAYER --> RISK_LAYER
  RISK_LAYER --> OPS_LAYER
  GATE_LAYER -.-> DATA_LAYER
  GATE_LAYER -.-> ENGINE_LAYER
  GATE_LAYER -.-> STRATEGY_LAYER
  GATE_LAYER -.-> RISK_LAYER
  GATE_LAYER -.-> OPS_LAYER
```

---

## 3. 核心子系统与物理防线说明

### 3.1 数据与时钟隔离 (反前视偏差)
1. **先撮合、后信号**: 引擎日内时钟强制锁定先撮合 T 日挂单（按 T+1 开盘价成交），再生成 T+1 交易信号，从物理时序上根除日内未来函数；
2. **点截面 (PIT) 财务与分红**: 严格采用 `pubDate`（公告披露日）对齐财务数据与分红除权预案，绝对禁止使用报告期 `statDate`；
3. **三源比对校验**: 主数据源与腾讯/新浪行情交叉核验，停牌日自动剔除并锁定市值平直，严禁前收价平推脏数据。

### 3.2 撮合引擎与真实摩擦 (反虚假盈利)
1. **七科目精确费用**:
   - 佣金: 万 2.5，单笔最低 5 元硬地板（单笔交易低于 2 万元时摩擦显著上升）；
   - 印花税: 卖出单边征收（严格按 2023-08-28 减半政策 0.5‰ 分段计税）；
   - 过户费: 沪深交易所双边 0.01‰；
   - 经手费与证管费: 独立科目精确到分，ROUND_HALF_UP 四舍五入；
2. **阶梯红利差别化个人所得税**:
   - 持股 < 1 个月: 20% 红利税；
   - 持股 1~12 个月: 10% 红利税；
   - 持股 1 年以上: 0% 免税；
   - FIFO 批次配对追溯持股天数，实扣税款计入账本流水。

### 3.3 组合与风控逻辑
1. **选股标的池初筛**: 全市场**停牌剔除** + TTM 股息率 >= 3% 纳入备选；⛔ ST / 退市整理股**未被剔除**（代码无该过滤逻辑，已如实更正，登记为待办）；
2. **持仓约束**: 持仓 3~8 只（默认 5 只），单票持仓市值 >= 2 万元，彻底平抑 5 元佣金磨损；
3. **MA200 择时空仓避险**: 以沪深 300 指数为基准，指数收盘价低于 MA200 时触发清仓信号，强制 100% 空仓持有现金，彻底规避系统性股灾。

### 3.4 自动化防伪门禁体系
1. **六维防御门禁**: 涵盖 D（数据）、L（活性）、E（引擎）、A（会计）、S（科学）、G（治理）全维度；数量**真值以 `scripts/gates/gate_master_audit.py` 的门禁注册表为单一事实源**（⛔ 文档不硬编码具体门禁数；2026-09-10 当日实测快照 29，工程侧仍在增门禁）；实测状态 12 PASS / 13 INCONCLUSIVE（无证据 ≠ 通过）/ 3 FAIL，⛔ 原文「24 道全绿」不成立；
2. **密码学防篡改签名**: 模拟盘/回测记录内嵌 Git Commit SHA、Data Hash、Timestamp 与指标哈希签名，防事后篡改；
3. **双仓硬拦截**: Git Hooks (`pre-commit` 与 `pre-push`) 与 GitHub Actions CI 实行全测试基线防倒退拦截（阈值**真值以 `scripts/gates/constants.py` 的 `TEST_BASELINE_PASSED` 为单一事实源**；⛔ 文档不硬编码具体数值）。

---

## 4. 程序化交易报备合规指标对照

| 指标项目 | 本系统实测值 | 交易所/高频认定标准 | 合规判定 |
| :--- | :--- | :--- | :--- |
| **策略类型** | 中低频价值红利 + 趋势空仓择时 | 普通程序化交易 | **合规** |
| **最高申报速率** | 峰值约 **0.2 笔/秒**（日终委托不超过 10 笔） | 高频认定：>= 300 笔/秒 或 >= 5 笔/秒 | **远低于高频线（合规）** |
| **单日最高申报笔数** | 极端调仓日峰值 **<= 20 笔**（持仓 8 只） | 高频认定：单日 >= 20,000 笔 | **远低于高频线（合规）** |
| **撤单率** | **恒为 0%**（中低频日终成交后不撤单） | 异常交易监控：撤单率 > 50% | **零撤单（绝对安全）** |
| **申报席位** | 本地普通个人工作站通过托管券商代报 | 报备时载明登记 | **合规** |
| **杠杆与做空工具** | 纯多头现货股票，无股指期货、无期权、无融资融券 | 杠杆比例受限 | **无杠杆纯多头（合规）** |

---

## 5. 报备审签结论

⛔ **原「完全符合报备标准」结论已撤回**。2026-09-10 复核后的如实结论：交易频率极低、零撤单、底层分级账本完备等结构性事实成立；但**门禁实测非全绿**（12 PASS / 13 INCONCLUSIVE / 3 FAIL）、权威回测产物**未通过**回撤上限门禁 `G-MDD-1`、告警通道尚未实现、ST 剔除能力缺失，且 Phase 4 为「⏸ 暂停」。故本说明书**尚不构成合规凭证**，须待上述项闭环后由纪律督查官重新审签。
