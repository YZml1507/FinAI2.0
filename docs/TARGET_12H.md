# FinAI2.0 — 十二小时冲刺任务文档（交 Hermes 自执行）

> 创建：2026-09-16 00:55 ｜ 生效区间：未来 12 小时 ｜ 授权：用户全权委托 AI 执行（2026-09-16 00:53）
> 事实源：docs/TASK_TRACKER.md（跨窗口唯一事实源）；本目标文档只是阶段分解，与其冲突时以 TASK_TRACKER 为准。

## 当前阶段结论（以最新榜单全量 24 组实测为准）
- 宽度参数网格：全部 24 组跑完，代码指纹 c944917 全程干净，无残留进程。
- ✅ 已出现 7 个达标组合（MDD < 35% 且 CAGR > 0），最优：bd25a45m00i1（MDD 21.43% / CAGR +3.76% / 胜率 53.00% / 换手 330.9%）；其余依次为 bd25a45m30i1（22.63%/+4.30%）、bd25a35m00i1（31.28%/+5.82%）、bd25a35m30i1（31.32%/+5.40%）、bd25a40m30i1（32.21%/+3.20%）、bd25a40m00i1（32.57%/+2.33%）、bd25a45m00i2（33.44%/+1.22%）。
- 关键规律：所有达标组 defense 阈值均为 0.25（defense 0.20→0.25 使平均 MDD 降 6.58pp、平均 CAGR 升 3.92pp），印证 P0 诊断『defense 提前避险』预测。
- 因此 Phase D 主线推进到『把最优宽度组合推入六维门禁，晋级候选基线』；PEAD 按既定决策排本期最后（alpha 增强，非主线）。

## 十二小时总目标
1. 走完主线：达标宽度组合 → 六维门禁 → 候选基线固化；
2. 清完收尾：哨兵刷屏修复、指纹 dirty 口径修复、全量回归 + commit；
3. 喂满算力：主线间隙绝不让服务器空转，用填充任务池持续产出研究数据。

## 主线任务清单（严格按序执行，完一件勾一件）

### 任务 A（主线，约 2~3 小时）：达标组合六维门禁验证与基线固化
A1. 对最优组合 bd25a45m00i1 做全量六维门禁复核并落盘结果；任一门禁红灯则按序换下一组（bd25a45m30i1 → bd25a35m00i1 → bd25a35m30i1 → bd25a40m30i1 → bd25a40m00i1 → bd25a45m00i2）。
A2. 通过门禁的组合定为候选基线，生成新基线产物（含出处三件套），把 tests 里的权威指针从 MA200 时代切换到该宽度基线。若 7 组全部不过门禁，立即发一条升级飞书并停止后续主线，仅保留填充池。
A3. 写不超过 120 字的 SELECTION.md 存入产物目录，说明选择理由（MDD/CAGR/胜率/换手四指标 + 与 P0 诊断对齐）。

### 任务 B（主线配套，约 2 小时）：宽度口径正式工程接入
B1. P2-b：在 run_dividend_backtest.py 的 _build_post_run_gate_context 注入 breadth_series 等宽度字段，使回测产出侧完整携带宽度口径 context。
B2. P2-c：为 S-2 宽度口径新增测试用例（范围以 TASK_TRACKER 既定为准），新测试必须全绿。

### 任务 C（清尾降噪，约 1 小时）
C1. 修 scripts/lab/sentinel.sh（用户点名的最高优先级）：① 榜单行解析失败的兜底分支不得原样透传整行 JSON；② 实验完成消息剔除/截断 breadth_series 等巨型参数字段，只保留策略关键参数。改完用最近一条真实榜单行做发送演练验证消息体干净。
C2. 修 _git_code_hash：dirty 哈希只覆盖代码路径（strategy/scripts/backtest/tests），榜单与文档追加不再污染指纹；改后验证『仅榜单追加时指纹不变、代码改动时指纹必变』两个方向。

### 任务 D（评估判断，约 30 分钟）
D1. 装甲一按 Hermes 修正后的立项理由（S-4 事前化的确定性收益，真实年化仅 +0.012pp~+0.3pp）重新评估：收益不值得则撤销立项并写明理由；仍值得则给出最小实施方案。两者只选其一，不许悬置。结论写入 docs/ARMOR1_EXDIV_FILTER_DESIGN.md 尾部并同步 TASK_TRACKER。

### 任务 E（固化，约 30 分钟）
E1. 全量回归 .venv/bin/pytest tests/ -q 必须全绿（基线 1022 条）。
E2. git commit 固化全部代码改动，提交信息精准列明改动项；回写 TASK_TRACKER 勾选完成项并更新时间戳。
E3. 发一条总结飞书：『十二小时主线完成，基线=<组合名>，回归全绿，噪声已除』。

### 任务 F（保底，仅在 A~E 全完成且时间有富余时启动）
F1. PEAD 探路实验启动准备：基于 financial_pit 数据（487只×16891行、pub_date 零前视、缺扣非字段先归母口径），搭探路实验骨架。时间不够则直接保留到下一阶段，不强推。

## 填充算力任务池（主线任何等待间隙自动插入，绝不让机器空转）
池内任务全部为『只读数据 + 新写研究产物』，不碰主线代码文件，不污染指纹，可随时被主线抢占，产物一律落 experiments/lab/ 或 docs/，实验名带 pool 前缀与主线区分。任一时刻最多并行 1 项池任务，不与主线回测同时抢 CPU。顺序：POOL-1 → POOL-3 → POOL-2 → POOL-4。

- POOL-1 最优点邻域细扫：围绕 bd25a45m00i1（defense=0.25/attack=0.45/mid_cap=0.0/ice=1）做 ±0.01/±0.02 细颗粒度网格，每组合一次完整回测，目标把 MDD 21.43% 往 20% 以内推，产出『最终推荐参数表』存 docs/。
- POOL-3 跨周期稳健性对照：bd25a45m00i1 参数在 2015-2018 / 2019-2021 / 2022-2024 / 2024-至今 四个子区间各跑一次回测，产出子区间指标矩阵存 docs/，验证不是单周期运气。
- POOL-2 PEAD 信号面预研：归母口径业绩公告事件信号的滚动统计探针，产出『年度触发次数』与『触发后 5/10/20 日平均超额』两张表存 experiments/lab/，只产统计不开策略代码。
- POOL-4 装甲一事件研究：仅当 D1 结论为『值得做』时执行——487 只含除权标记股票在除权前 15 天禁建仓前后的净值对比数据落盘；D1 撤销则跳过。

## 通知规范（必须严格照做）
1. 每完成一个任务块（A/B/C/D/E/F 或任一 POOL）才发一条飞书，格式：『[FinAI2.0 任务完成] <任务编号> <一句话结果>』。
2. ⛔ 严禁任何消息携带 Decimal、日期字典、大段 JSON 原文；发送前必须确认消息体已去巨型字段（C1 完成前，涉及榜单内容的消息一律先存日志不发）。
3. 告警与完成汇报分开：疑似卡死/进程异常单独一条，不与完成通知混发。
4. 同一任务块的进展刷屏零容忍：除完成通知与告警外，禁止任何中间态飞书。

## 放权边界（给 Hermes 的行为底线）
1. 允许自行修复执行中发现的缺陷、改代码、写测试、调配置，但必须同时满足：
   - 不改任何任务的最终目标（门禁达标、基线晋级、噪声清除、指纹归一）；
   - 不改任务优先级顺序（主线 A→B→C→D→E，F 与填充池垫底）；
   - 不通过绕开或弱化 S-2/S-4 门禁的方式让任何测试『看起来』通过。
2. 若发现与本文件方向冲突、或会替换 Phase D 主线的重大事项，停止并立刻发一条升级请示飞书，不准自行拍板。

## 执行机制（跳板调度）
由调度器按本文件顺序把每个任务块作为独立指令逐条下发执行；每块执行完毕收到完成信号后自动推进下一块，并发出对应的完成飞书；主线间隙自动插入填充池任务。执行全程在独立会话进行，与网格、监工、巡检互不干扰。
