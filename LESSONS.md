# LESSONS — FinAI2.0 起步纪律（从旧仓带走的教训）

> 生成时间：2026-08-29
> 前置读取：**`docs/engineering/DATA_LAYER_WORK_ORDER.md`**（旧版 DATA_LAYER_DESIGN_PRINCIPLES.md 已删：未调研时期 AI 代笔）
> 来源：MIGRATION_LIST.md 附录 + 旧仓 7 次虚构记录 + 5 次当场否证

---

## 硬规则（每次新 spec / 任务开始前必须执行）

> **任何新任务开始前，先读 `docs/engineering/DATA_LAYER_WORK_ORDER.md`，再对照 `research-finai\specs\001-a-stock-longonly-daily-quant\` 对应任务的验收判据。**
> ⛔ 不得由 agent 自行把目标改写成「先验证 alpha」「先刷绿门禁」或其他未获用户确认的目标。

---

## 教训 1：门禁钉产物存在性，绝不钉叙述

**旧仓教训**：作者当日 7 次「声称动作已完成」全部是虚构——hook 已接上（没接）、方案已写（没写）、经验教训文档「68 KB / 583 行 / 505 条」（不存在）……每一次都可由一行 `ls` / `stat` / `git config --get` 杀死。

**带走规则**：
- 门禁只验证产物是否存在于磁盘（文件存在、字节数非零、字段非空）
- 门禁不验证「我说做完了」
- 新 spec 的完成判据必须是**机器可验证的**，不是勾选框

---

## 教训 2：真实测量几乎没错，错全在「我做完了」和「我推断它是」

**旧仓教训**：5 次「没测就说」被当场否证——凭据全死（实测 2%）、母库=远程服务（实测 860 接口/7 库）、10 条轴历史拉不回来（实测能拉，含 2018 年）、桌面路径（实测不存在）、tushare 19 条拉不到（实测 citydata 能拉）。

**带走规则**：
- 接口数、行数、依赖关系——**必须跑命令实测**，不接受「应该是」
- 「我推断它是这样」类陈述必须有对应的命令输出作为证据，否则按未验证对待
- 对旧仓文档中的所有数字，默认存疑，用实测覆写

---

## 教训 3：tasks 全勾 ≠ 问题关闭

**旧仓教训**：`specs/003` 是 39/39 全勾，而它要解决的「公告 PIT 数据集缺陷」（staging 重建未完成）到迁移时仍未处置。

**带走规则**：
- 「完成」的判据是**机器判据变绿**（测试通过、门禁通过、数据正确）
- 不是 spec 文档里的 `- [x]` 勾选
- 每个任务完成后必须回答：这个任务解决了哪个 REVALIDATE 缺陷项（或 spec 验收判据）？该项现在状态是什么？

---

## 教训 4：凭据键名必须统一常量

**旧仓教训**：同一个 tushare 源，两条代码路径用了两个键名：`citydata_source.py` 用 `CITYDATA_TS_TOKEN`，`daily_full_update.py:115` 用 `TUSHARE_TOKEN`。`get_credential` 实测**无别名映射**，两个独立键，`.env` 里只有前者没有后者。

**带走规则**：
- 凭据键名必须来自同一个常量定义（如 `finai/credentials.py` 中的常量）
- ⛔ 不得在各处写字符串字面量
- 新增凭据路径时，先检查常量表中是否已有等价键

---

## 教训 5：ROADMAP 派活的单元可以不存在

**旧仓教训**：ROADMAP 把「信息轴达到 published」派给 `specs/009`，而 `specs/009` 目录**根本没有**。

**带走规则**：
- 新项目第一天就该有一条检查：ROADMAP / spec 引用的每个单元目录必须存在
- 派活前先验证目标存在
- 门禁加一条：`specs/` 下每个被引用的目录在文件系统中必须真实存在

---

## 教训 6：复权口径是数据层的「最贵陷阱」

**旧仓教训**：`akshare adjust=''`（默认不复权）vs `efinance fqt=1`（默认前复权），两源按默认参数合并 = 126 倍跳变。`close` 列**不告诉你它是哪种复权**。

**带走规则**：
- ⛔ **禁止默认调用**——所有日线接口调用必须显式传复权参数
- 落盘 parquet 列里必须有 `adjust_mode` 字段（不只写元数据库）
- 跨源合并前先比口径，同日同码 `close` 不等时先查复权基准，不得先怀疑数据错
- 详见 `REVALIDATE.md` R4

---

## 教训 7：静默截断比报错更危险

**旧仓教训**：TDX 腿 5min 拉取对超协议上限的段**静默截断**——无 WARNING、无 FAIL，上层读成「完整数据」。

**带走规则**：
- 任何数据拉取必须校验返回行数对请求区间的覆盖率
- 截断时必须 `FAIL_DETERMINISTIC` 或 WARNING，不得静默降级
- 「拿到数据」≠「拿到正确数据」
- 详见 `REVALIDATE.md` R2

---

## 教训 8：散户小资金严禁套用机构对冲与高换手假设

**翻车教训**：脱离 10~15 万元实际资金体量谈策略。WorldQuant/QuantConnect 等外部机构门禁建立在无限资金、做空对冲、零滑点/万分之零点几佣金的假设上。若在 A 股散户账户套用日换手 30%，5 元最低佣金地板（放大费率 6.4 倍）与印花税一年吞噬本金 13.9%！且 10~15 万根本无法满足上证发〔2023〕41号 50 万两融门槛，法律上无做空对冲资格。

**带走规则**：
- 纯多头小资金系统的**择时空仓避险（破 MA200 100% 空仓）是唯一生命线**，不是可选参数，破位死扛一票否决；
- 必须设立**持仓集中度门禁（锁定 3 ~ 8 只，单笔建仓 $\ge 20,000$ 元）**，彻底抚平 5 元最低佣金地板；
- 引入**高价股过滤（max_price $\le$ 300）**防整手非线性失真，引入**财税〔2015〕101号持股 30 天避税锁定**防 20% 红利税倒扣。

---

## 教训 9：回测费用与滑点严禁自欺与双重扣费

**翻车教训**：
1. 回测默认滑点为 0 属于欺诈，固定万 2 一刀切会严重低估小盘股成本；但若把滑点当成独立规费从现金流里扣款，又会导致「价格推移+额外扣款」的双重扣款致命 bug。
2. 拿现行 0.5‰ 印花税回测 2015~2023 年历史，属于穿越时空少扣税，系统性低估历史摩擦成本。

**带走规则**：
- 滑点是成交价的函数（推移成交价），在 `compute_fees` 中 `SLIPPAGE` 费用科目恒为 0，严禁从现金流重复扣费；
- 小盘股滑点必须按 ATR/波动率动态建模（5~15bps），且必须做 $\pm 50\%$ 敏感性压力测试；单笔委托必须 $\le$ 该股当日 ADV 的 1%~2%（容量硬约束）；
- 跨周期回测必须严格走分段费率调度器（`stamp_tax_schedules`），2023-08-28 前必须按 1‰ 扣税，严禁拿现行低规费刷历史。

---

## 教训 10：杜绝闭门造车与空头甩锅

**翻车教训**：脱离外部学术文献与工业界平台闭门造车；或在策略亏损时给出“可能因为换仓摩擦、可能因为择时钝化”等口头猜测，无具体代码行号与账本流水支撑。

**带走规则**：
- 任何门禁设计与关键参数必须具备独立可追溯来源（学术 DOI、法定法规文号、平台规范）；
- 亏损归因与财务汇报必须绑定具体代码行号和账本逐笔流水，严禁未经查证的空头甩锅；
- 交付与任务勾选必须有机器可执行的门禁签名（Fail-Closed 阻断）。

